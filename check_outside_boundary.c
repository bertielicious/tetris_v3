#include <xc.h>
#include<pic.h>
#include <stdbool.h>
#include "config.h"

bool check_outside_boundary(void)
{
    uchar row, col;
    for (row = 0; row < 16; row ++)
    {
        for ( col = 0; col < 8; col++)
        {
            
        }
    }
}
